import { Container } from "@mui/material";
import styles from "./Content.module.css";

const Content = () => {
  return <Container className={styles.content}>Main</Container>;
};

export default Content;
