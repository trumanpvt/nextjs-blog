import { Container } from "@mui/material";
import styles from "./Header.module.css";

const Header = () => {
  return <Container className={styles.header}>Header</Container>;
};

export default Header;
