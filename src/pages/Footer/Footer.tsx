import { Container } from "@mui/material";
import styles from "./Footer.module.css";

const Footer = () => {
  return <Container className={styles.footer}>Footer</Container>;
};

export default Footer;
